
	#git commands

		git clone
		git status
		git add .
		git add -A (adding all changes)
		git commit -m 'message'
		git commit (Esc:wq Enter)
		git push
		git pull



	#team work
		fork
		git clone
		pull request

		git branch
		git branch (name)
		git checkout (branch name)
		git merge (branch)